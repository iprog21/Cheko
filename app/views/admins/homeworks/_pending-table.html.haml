%tr
  %td= home.created_at.strftime("%b %d, %Y")
  %td= home.order_type.titleize
  %td= home.sub_type.titleize if home.sub_type.present?
  %td= home.deadline.strftime("%b %d, %Y - %l:%M %p") if home.deadline.present?
  %td= home.subject.titleize if home.subject.present?
  %td= home.tutor_category.titleize if home.tutor_category.present?
  %td= home.words
  %td= home.tutor_samples.to_s.capitalize
  %td= home.tutor_skills.to_s.capitalize
  %td= home.view_bidders.to_s.capitalize
  %td= home.priority.to_s.capitalize
  %td= home.login_school.to_s.capitalize
  %td
    %a{href: admins_homework_path(home)}
      .fas.fa-eye.text-primary.font-16.mr-3
    - if home.admin_id == current_admin.id || current_admin.role == 'super_admin'
      %a{href: edit_admins_homework_path(home)}
        .fas.fa-pen.text-secondary.font-16.mr-3
      %a{href: admins_homework_assign_path(homework_id: home.id), data: {method: :put}}
        .fas.fa-circle-check.text-success.font-16.mr-3
    - if current_admin.role == 'super_admin'
      %a.fas.fa-trash.text-danger.font-16.mr-3{href: admins_homework_path(home), data: {method: :delete}}